const EjemploProps1 = props => {

    return(
        <h3>Hola {props.nombre}</h3>
    )
}

export{
    EjemploProps1
    //acá usamos {} y no export default porque son varios componentes distintos lo que vamos a exportar
}